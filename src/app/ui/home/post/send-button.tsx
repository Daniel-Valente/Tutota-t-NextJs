import React from "react";
import { Send } from "@mui/icons-material";

export const SendButton = () => {
    return (
        <button className="">
            <Send sx={{ fontSize: 30 }} />
        </button>
    );
};
